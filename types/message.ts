export type CustomUIDataTypes = {
  sandboxHost: {
    host: string;
  };
  code: {
    filePath: string;
    code: string;
  };
  codeGenerationStarted: {
    started: boolean;
  };
  id: {
    id: string;
  };
};
